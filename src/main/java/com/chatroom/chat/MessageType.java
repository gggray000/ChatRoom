package com.chatroom.chat;

public enum MessageType {
    CHAT,
    JOIN,
    LEAVE,
    USER_LIST,
    TYPING,
    TYPING_STOPPED,
    GET_HISTORY,
    SHOW_HISTORY,
    GENERATE_SUMMARY,
    SUMMARY,
    END
}
