package com.chatroom.chat;

public enum MessageType {
    CHAT,
    JOIN,
    LEAVE,
    USER_LIST,
    TYPING,
    TYPING_STOPPED,
    TIMER_START,
    UPDATE_TIME,
    TIMER_PAUSE,
    TIMES_UP,
    GET_HISTORY,
    SHOW_HISTORY,
    GENERATE_SUMMARY,
    SUMMARY,
    SHUTDOWN
}
