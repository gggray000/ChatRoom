package com.chatroom.chat;

public enum MessageType {
    CHAT,
    JOIN,
    LEAVE,
    USER_LIST,
    TYPING,
    TYPING_STOPPED,
    UPDATE_TIME,
    TIMER_START,
    TIMER_PAUSE,
    GET_HISTORY,
    SHOW_HISTORY,
    GENERATE_SUMMARY,
    SUMMARY,
    SHUTDOWN
}
